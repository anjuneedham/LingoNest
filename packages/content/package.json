{
  "name": "@lingonest/content",
  "version": "0.1.0",
  "private": true,
  "description": "Curriculum as data: a typed authoring DSL plus the CEFR curricula for the launch languages.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "validate": "tsx scripts/validate.ts",
    "build": "tsx scripts/build.ts"
  },
  "dependencies": {
    "@lingonest/core": "*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.20.3",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
